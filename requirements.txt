# 肺影智诊 · 罕见病临床决策支持系统
# Python 3.12（Windows / Anaconda），版本号与当前已验证环境一致

# Web 框架
Flask==3.0.3
Flask-Login==0.6.3
Werkzeug==3.1.3

# 数据库
mysql-connector-python==9.4.0

# 深度学习（模型推理 + 训练）
torch==2.8.0
torchvision==0.23.0
numpy==1.26.4

# 影像处理
Pillow==10.4.0
pydicom==3.0.2
pylibjpeg==2.1.0
pylibjpeg-libjpeg==2.4.0

# 报告导出（PDF）
reportlab==5.0.0

# 数据与训练辅助
matplotlib==3.9.2
pandas==2.2.2
scikit-learn==1.5.1
scipy==1.13.1

# 训练 MAML 模型时额外需要（推理不需要）
learn2learn
